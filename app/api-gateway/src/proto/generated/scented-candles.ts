// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.12.0
//   protoc               v7.35.1
// source: scented-candles.proto

/* eslint-disable */
import { GrpcMethod, GrpcStreamMethod } from "@nestjs/microservices";
import { Observable } from "rxjs";

export const protobufPackage = "scented_candles.ai.v1";

export interface ChatRequest {
  userId: string;
  message: string;
  /** Ngữ cảnh câu hỏi trước đó */
  chatHistory: string[];
}

export interface ChatChunk {
  /** Từng cụm từ trả về real-time */
  deltaText: string;
  /** Cờ báo hiệu kết thúc stream */
  isFinished: boolean;
}

export interface SearchQuery {
  /** VD: "nến thơm mùi gỗ ấm cho phòng ngủ buổi tối" */
  query: string;
  /** Số lượng sản phẩm tối đa trả về */
  limit: number;
}

export interface CandleProduct {
  id: string;
  name: string;
  price: number;
  imageUrl: string;
  /** Điểm khớp ngữ nghĩa (Score) */
  similarityScore: number;
}

export interface SearchResponse {
  products: CandleProduct[];
}

export interface ExtractRequest {
  rawDescription: string;
}

export interface ExtractResponse {
  /** Hương đầu (VD: ["Chanh", "Cam"]) */
  topNotes: string[];
  /** Hương giữa (VD: ["Oải hương"]) */
  middleNotes: string[];
  /** Hương cuối (VD: ["Gỗ thông", "Hổ phách"]) */
  baseNotes: string[];
  /** Không gian / Cảm xúc (VD: ["Thư giãn", "Phòng ngủ"]) */
  moods: string[];
}

export const SCENTED_CANDLES_AI_V1_PACKAGE_NAME = "scented_candles.ai.v1";

/** Service xử lý toàn bộ logic AI & Search nội bộ */

export interface ScentedCandlesAIServiceClient {
  /** 1. Chatbot Tư Vấn (Server Streaming RPC): Trả về từng đoạn chữ real-time (hiệu ứng typing) */

  streamAiChat(request: ChatRequest): Observable<ChatChunk>;

  /** 2. Hybrid Search (Simple RPC): Kết hợp Full-text (Elasticsearch) + Vector (Qdrant) */

  smartSearch(request: SearchQuery): Observable<SearchResponse>;

  /** 3. Metadata Extraction (Simple RPC): Bóc tách thuộc tính nến thơm khi Admin tạo sản phẩm */

  extractCandleMetadata(request: ExtractRequest): Observable<ExtractResponse>;
}

export interface ScentedCandlesAIServiceController {
  
  streamAiChat(request: ChatRequest): Observable<ChatChunk>;

  smartSearch(request: SearchQuery): Promise<SearchResponse> | Observable<SearchResponse> | SearchResponse;

  extractCandleMetadata(
    request: ExtractRequest,
  ): Promise<ExtractResponse> | Observable<ExtractResponse> | ExtractResponse;
}

export function ScentedCandlesAIServiceControllerMethods() {
  return function (constructor: Function) {
    const grpcMethods: string[] = ["streamAiChat", "smartSearch", "extractCandleMetadata"];
    for (const method of grpcMethods) {
      const descriptor: any = Reflect.getOwnPropertyDescriptor(constructor.prototype, method);
      GrpcMethod("ScentedCandlesAIService", method)(constructor.prototype[method], method, descriptor);
    }
    const grpcStreamMethods: string[] = [];
    for (const method of grpcStreamMethods) {
      const descriptor: any = Reflect.getOwnPropertyDescriptor(constructor.prototype, method);
      GrpcStreamMethod("ScentedCandlesAIService", method)(constructor.prototype[method], method, descriptor);
    }
  };
}

export const SCENTED_CANDLES_AI_SERVICE_NAME = "ScentedCandlesAIService";
